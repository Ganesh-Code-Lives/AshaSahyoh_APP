name: iOS IPA Build

on:
  workflow_dispatch:

jobs:
  build-ios:
    name: Build iOS IPA
    runs-on: macos-latest

    steps:
      # Checkout code
      - name: Checkout
        uses: actions/checkout@v4

      # Setup Flutter
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      # Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # Clean (IMPORTANT - avoids cache issues)
      - name: Clean project
        run: flutter clean

      # Install CocoaPods
      - name: Install CocoaPods
        run: |
          cd ios
          pod install

      # Build IPA (FIXED)
      - name: Build IPA
        run: flutter build ipa --release --export-method ad-hoc

      # Debug (VERY IMPORTANT)
      - name: List build files
        run: ls -R build/ios

      # Upload IPA
      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: |
            build/ios/ipa/*.ipa
            build/ios/archive/*.xcarchive
